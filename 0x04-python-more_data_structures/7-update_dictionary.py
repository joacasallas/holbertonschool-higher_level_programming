#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    a_dictionary[key] = value
    new_a_directory = {}
    for key in a_dictionary:
        print(key+":", a_dictionary[key])
        new_a_directory.get(key)
    return(new_a_directory)
